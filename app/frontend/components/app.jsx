const React = require('react');

var App = React.createClass({
  render: () => {
    return (
      <div className='container'>
        <h1>Hello, world!</h1>
      </div>
    );
  }
})

module.exports = App;